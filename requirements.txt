# Core RAG app. Everything the service and the evaluation harness need.
#
# Versions are pinned so an image built today and one built next month contain the same
# code. Unpinned ranges made the container non-reproducible, which undermined every
# published number that depended on it.
#
# Fine-tuning dependencies are separate (finetune/requirements.txt): that notebook runs on
# Colab, not here.
#
# The "hf" generation backend additionally needs torch, transformers and peft. They are
# deliberately not listed - they are large and the served system does not use them.
fastapi==0.141.1
uvicorn[standard]==0.52.1
qdrant-client==1.19.0
sentence-transformers==5.7.0
python-dotenv==1.2.2
requests==2.34.2
pydantic==2.13.4
