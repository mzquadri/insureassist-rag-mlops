# Core RAG app. Everything the service and the evaluation harness need.
#
# Versions are pinned so an image built today and one built next month contain the same
# code. Unpinned ranges made the container non-reproducible, which undermined every
# published number that depended on it.
#
# Fine-tuning dependencies are separate (finetune/requirements.txt): that notebook runs on
# Colab, not here.
#
# The "hf" generation backend additionally needs peft, which is deliberately not listed:
# the served system calls Ollama and never loads it.
#
# torch and transformers are not optional and never were. sentence-transformers requires
# both, so installing this file installs them transitively. The note here used to group
# all three as "deliberately not listed", which read as though a plain install stayed
# small; it does not, and a reader sizing the image should know that.
fastapi==0.141.1
uvicorn[standard]==0.52.1
qdrant-client==1.19.0
sentence-transformers==5.7.0
python-dotenv==1.2.2
requests==2.34.2
pydantic==2.13.4
