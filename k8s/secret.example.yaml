# Copy to secret.yaml and fill real values. NEVER commit secret.yaml (it's git-ignored).
# Create it on the cluster with:  kubectl apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: insureassist-secrets
type: Opaque
stringData:
  # Only needed if you use LangSmith tracing or an OpenAI judge for RAGAS.
  LANGSMITH_API_KEY: "replace-me"
  OPENAI_API_KEY: "replace-me"
